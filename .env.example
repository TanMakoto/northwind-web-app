# Database Configuration (Local Development)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=dbnorthwind
DB_USER=root
DB_PASS=

# Note: On Railway PaaS, environment variables (MYSQLHOST, MYSQLUSER, MYSQLPASSWORD, MYSQLDATABASE, MYSQLPORT)
# are automatically provided by the Railway MySQL plugin, so no manual .env configuration is needed!
